{
  "name": "site",
  "version": "1.0.0",
  "description": "MagicLight AI Review & Invitation Code Static Hub",
  "main": "index.js",
  "scripts": {
    "build": "tailwindcss -i input.css -o css/styles.css --minify",
    "watch": "tailwindcss -i input.css -o css/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}